services:
  backend:
    image: codesnipgo
    container_name: codesnipgo
    ports:
      - "8080:8080"
    env_file:
      - ./backend/.env

  frontend:
    image: codesnipfe
    container_name: codesnipfe
    ports:
      - "3000:3000"
    env_file:
      - ./frontend/.env

